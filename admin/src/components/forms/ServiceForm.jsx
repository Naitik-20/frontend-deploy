import { useAdminDashboard } from '../../hooks/useAdminDashboard';
export default function ServiceForm() {
  const { serviceForm, editingService, handleServiceChange, handleSubmitService, resetServiceForm } = useAdminDashboard();
  return <form className="admin-form-pane" onSubmit={handleSubmitService}><div className="form-section-title">{editingService ? 'Edit Service' : 'Add Service'}</div>{['title','slug','price','duration'].map((f) => <div className="form-group" key={f}><label>{f}</label><input className="form-input" name={f} value={serviceForm[f] || ''} onChange={handleServiceChange}/></div>)}<div className="form-group"><label>Description</label><textarea className="form-textarea" name="description" value={serviceForm.description} onChange={handleServiceChange}/></div><div className="form-group"><label>Features</label><textarea className="form-textarea" name="features" value={serviceForm.features} onChange={handleServiceChange}/></div><label className="checkbox-row"><input type="checkbox" name="isActive" checked={serviceForm.isActive} onChange={handleServiceChange}/> Active</label><div className="form-action-bar"><button type="button" className="dashboard-clear-btn" onClick={resetServiceForm}>Clear</button><button className="dashboard-submit-btn">Save Service</button></div></form>;
}
